<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
    <title>Hello World | 经典欢迎页</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: system-ui, 'Segoe UI', 'Roboto', 'Helvetica Neue', sans-serif;
            background: linear-gradient(145deg, #f0f4ff 0%, #e6edf7 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 1.5rem;
            /* 优雅的滚动行为 */
            scroll-behavior: smooth;
        }

        /* 主卡片容器 */
        .hello-card {
            max-width: 680px;
            width: 100%;
            background: rgba(255, 255, 255, 0.96);
            backdrop-filter: blur(0px);
            border-radius: 3rem;
            box-shadow: 0 25px 45px -12px rgba(0, 0, 0, 0.25), 0 2px 6px rgba(0, 0, 0, 0.05);
            text-align: center;
            padding: 3rem 2rem 3.5rem 2rem;
            transition: transform 0.25s ease, box-shadow 0.3s ease;
            border: 1px solid rgba(255, 255, 255, 0.5);
        }

        .hello-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 30px 55px -12px rgba(0, 0, 0, 0.3);
        }

        /* 装饰性光晕元素 */
        .glow-text {
            font-size: 5rem;
            line-height: 1.1;
            font-weight: 800;
            background: linear-gradient(135deg, #1e2b6e, #2a3f8f, #3b5bb5);
            background-clip: text;
            -webkit-background-clip: text;
            color: transparent;
            margin-bottom: 0.75rem;
            letter-spacing: -0.02em;
            text-shadow: 0 2px 5px rgba(0, 0, 0, 0.05);
        }

        /* 辅助欢迎语 */
        .welcome-sub {
            font-size: 1.4rem;
            font-weight: 500;
            color: #2c3e66;
            margin-bottom: 1rem;
            letter-spacing: -0.2px;
            border-bottom: 2px dashed #cbd5e6;
            display: inline-block;
            padding-bottom: 0.3rem;
        }

        .message {
            font-size: 1.1rem;
            color: #2c3e66;
            background: #f8fafd;
            padding: 1.2rem 1.8rem;
            border-radius: 2rem;
            margin: 1.8rem 0 1.2rem 0;
            line-height: 1.5;
            font-weight: 450;
            border: 1px solid rgba(59, 91, 181, 0.15);
            box-shadow: inset 0 1px 2px rgba(0,0,0,0.02), 0 1px 2px rgba(0,0,0,0.02);
        }

        .emoji-deco {
            font-size: 3rem;
            margin-top: 0.5rem;
            display: inline-block;
            filter: drop-shadow(0 4px 6px rgba(0, 0, 0, 0.1));
        }

        .footer-note {
            margin-top: 2rem;
            font-size: 0.85rem;
            color: #5b6e8c;
            border-top: 1px solid #e2e8f0;
            padding-top: 1.5rem;
            display: flex;
            justify-content: center;
            gap: 1rem;
            flex-wrap: wrap;
        }

        .footer-note span {
            background: #eef3fc;
            padding: 0.2rem 0.8rem;
            border-radius: 40px;
            font-weight: 450;
        }

        /* 简约动画，让hello字眼微光闪烁 */
        @keyframes gentlePulse {
            0% { opacity: 0.9; text-shadow: 0 0 0px rgba(30, 43, 110, 0.2);}
            100% { opacity: 1; text-shadow: 0 0 8px rgba(59, 91, 181, 0.3);}
        }

        .glow-text {
            animation: gentlePulse 2s infinite alternate ease-in-out;
        }

        /* 响应式调整 */
        @media (max-width: 550px) {
            .hello-card {
                padding: 2rem 1.5rem 2.5rem 1.5rem;
                border-radius: 2rem;
            }
            .glow-text {
                font-size: 3.6rem;
            }
            .welcome-sub {
                font-size: 1.2rem;
            }
            .message {
                font-size: 1rem;
                padding: 1rem 1.2rem;
            }
            .emoji-deco {
                font-size: 2.4rem;
            }
        }

        @media (max-width: 380px) {
            .glow-text {
                font-size: 2.8rem;
            }
        }

        /* 定制选中样式 */
        ::selection {
            background: #3b5bb5;
            color: white;
        }
    </style>
</head>
<body>
    <div class="hello-card">
        <!-- 主标题：Hello World 醒目展示 -->
        <div class="glow-text">
            Hello, World!
        </div>
        <!-- 亲切副标题 -->
        <div class="welcome-sub">
            🌟 欢迎来到我的页面 🌟
        </div>
        <!-- 描述性信息，增加一点趣味 -->
        <div class="message">
            👋 这是您的第一个HTML页面。<br>
            “代码不仅仅是逻辑，更是创造美好的语言。”<br>
            <span style="font-size:0.9rem; display:inline-block; margin-top:8px;">✨ 保持好奇，继续探索 ✨</span>
        </div>
        <!-- 装饰emoji，传达友好 -->
        <div class="emoji-deco">
            🚀 💻 🌈
        </div>
        <!-- 底部干净状态栏信息，简洁技术元素 -->
        <div class="footer-note">
            <span>📄 HTML5</span>
            <span>🎨 极简主义</span>
            <span>⚡ 响应式设计</span>
        </div>
    </div>
</body>
</html>